import React from 'react';
import {StyleSheet, Text, View } from 'react-native';

export default class ProfileScreen extends React.Component {
  render() {
    return (
      <View>
        <Text> Profile screen for Serve </Text>
      </View>
    );
  }
}

//refer to https://facebook.github.io/react-native/docs/network.html to import from database?
